require "spec_helper"

describe EtudeOp10No6 do
  it "has a version number" do
    expect(EtudeOp10No6::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
